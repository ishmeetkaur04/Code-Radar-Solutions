#include <stdio.h>

int main() {
    int decimal;
    scanf("%d", &decimal); 
    printf("Octal: %o\n", decimal, decimal);
    printf("Hexadecimal: %X\n", decimal, decimal);

    return 0;
}
