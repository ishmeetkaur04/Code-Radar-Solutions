#include <stdio.h>
int main(){
    int n,a,b;
    scanf("%d %d %d",&n,&a,&b);
    if (n>=18){
        printf("Eligible");
    }
  
    else {
        printf("Not Eliigible");
    }
    return 0;
}